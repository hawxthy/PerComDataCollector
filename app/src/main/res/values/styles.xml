<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/bright_foreground_material_light</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/abc_input_method_navigation_guard</item>
        <item name="android:textColorPrimaryInverse">@color/abc_search_url_text_selected</item>
        <item name="android:textColorSecondary">@color/abc_search_url_text_pressed</item>
        <item name="android:textColorSecondaryInverse">@color/abc_search_url_text_pressed</item>
        <item name="android:windowTitleBackgroundStyle">@color/abc_search_url_text_pressed</item>
    </style>

</resources>
